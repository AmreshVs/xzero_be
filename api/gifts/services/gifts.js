'use strict';

/**
 * Read the documentation (https://strapi.io/documentation/v3.x/concepts/services.html#core-services)
 * to customize this service
 */

module.exports = {
  // async resetGiftDraw(user = null) {
 
  //   let clearedGiftDraw = await strapi.query('membership').model.query(qb => {
  //     qb.where({is_gift_generated: true})
  //     }).save( {is_gift_generated: false}, {patch:true} ).then(function(x) {
  //     return true;
  //     })
  //   if(clearedGiftDraw === true) {
  //     return true
  //   } 
  //   return false;

  // }
};
