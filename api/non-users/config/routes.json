{
  "routes": [
    {
      "method": "GET",
      "path": "/non-users",
      "handler": "non-users.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/non-users/count",
      "handler": "non-users.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/non-users/:id",
      "handler": "non-users.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/non-users",
      "handler": "non-users.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/non-users/:id",
      "handler": "non-users.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/non-users/:id",
      "handler": "non-users.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
